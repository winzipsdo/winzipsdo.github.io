// eslint-disable-next-line no-unused-vars
import { createComponent as h } from '@vue/composition-api';
import Editor from '../../components/Editor';

export default {
  name: 'editor',
  render() {
    return (
      <div>
        <Editor />
      </div>
    );
  },
};
